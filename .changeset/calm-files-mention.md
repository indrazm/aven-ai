---
'aven-ai': minor
---

Add fuzzy `@` mentions for non-ignored project files and folders, with structured path references for agent prompts. Consolidate provider setup into `/connect`, remove the redundant `/commands` command, prevent agents from rediscovering automatically loaded project instructions, keep agent communication pragmatic and concise by default, and add vertical spacing around command execution rows.
